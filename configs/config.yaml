defaults:
  - _self_ 
  - model: decoder
  - training: default
  - experiment: base

seed: 42

data:
  train_path: data/3train.txt
  val_path: null
  val_split: 0.1

hydra:
  run:
    dir: ${experiment.output_dir}
  job:
    chdir: False
